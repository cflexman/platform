// Import Actions
import {  } from './SessionActions';

// Initial State
const initialState = {};

const SessionReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default SessionReducer;
